/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['1zaqtzgggf.execute-api.ap-southeast-2.amazonaws.com', 'media.discordapp.net', 'cdn.discordapp.com', 'localhost'],
    },
}

module.exports = nextConfig
