/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['nodejs-restfulapi-onlineshopdb.onrender.com', 'media.discordapp.net', 'cdn.discordapp.com', 'localhost'],
    },
}

module.exports = nextConfig
